process {
    container = 'tcell_workflow_updated:latest'

    executor = 'local' // Change to 'awsbatch', 'slurm', etc., for specific environments
    cpus = 2           // Default
    memory = '4 GB'    // Default
}

docker {
    enabled = true
}

